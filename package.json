{
  "name": "legend-murals",
  "version": "1.0.0",
  "description": "Legend Murals — Premium wall stickers & murals inspired by epic stories, history, and mythology.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-static-js.mjs && node scripts/validate-build.mjs",
    "preview": "vite preview",
    "audit": "node scripts/audit.mjs",
    "audit:strict": "node scripts/audit.mjs --strict",
    "audit:css": "node scripts/css-audit.mjs",
    "audit:media": "node scripts/media-audit.mjs",
    "audit:media:strict": "node scripts/media-audit.mjs --strict",
    "generate:products:poc": "node scripts/generate-product-poc.mjs",
    "validate:build": "node scripts/validate-build.mjs",
    "validate:products": "node scripts/validate-products.mjs",
    "test": "node --test tests/*.test.mjs",
    "quality": "npm run audit && npm run audit:css && npm run audit:media && npm run generate:products:poc && npm run validate:products && npm test && npm run build"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.18",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1",
    "terser": "^5.48.0",
    "vite": "^5.1.4"
  }
}
