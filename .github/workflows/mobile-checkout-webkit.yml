name: Mobile checkout WebKit regression

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mobile-checkout-webkit-${{ github.ref }}
  cancel-in-progress: true

jobs:
  webkit-regression:
    name: iPhone WebKit checkout regression
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install project dependencies
        run: npm ci

      - name: Build production storefront output
        run: npm run build

      - name: Install pinned Playwright WebKit for this test only
        run: |
          npm install --no-save --ignore-scripts playwright@1.55.0
          npx playwright install --with-deps webkit

      - name: Start production preview
        run: |
          set -euo pipefail
          npm run preview -- --host 127.0.0.1 --port 3001 > /tmp/legendmural-preview.log 2>&1 &
          echo $! > /tmp/legendmural-preview.pid

      - name: Wait for storefront
        run: |
          set -euo pipefail
          READY=0
          for attempt in $(seq 1 30); do
            if curl --fail --silent http://127.0.0.1:3001/shop.html > /dev/null; then
              READY=1
              break
            fi
            sleep 1
          done
          if [ "$READY" -ne 1 ]; then
            echo "Production preview did not become ready."
            cat /tmp/legendmural-preview.log || true
            exit 1
          fi

      - name: Run iPhone WebKit checkout regression
        env:
          TEST_BASE_URL: http://127.0.0.1:3001
        run: |
          set -o pipefail
          node tests/browser/mobile-checkout-webkit.mjs 2>&1 | tee /tmp/mobile-checkout-webkit.log

      - name: Upload diagnostics on failure only
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: mobile-checkout-webkit-diagnostics
          path: |
            /tmp/legendmural-preview.log
            /tmp/mobile-checkout-webkit.log
          if-no-files-found: warn
          retention-days: 1

      - name: Stop production preview
        if: always()
        run: |
          if [ -f /tmp/legendmural-preview.pid ]; then
            kill "$(cat /tmp/legendmural-preview.pid)" 2>/dev/null || true
          fi
