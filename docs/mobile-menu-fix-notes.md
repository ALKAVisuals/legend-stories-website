# Mobile menu viewport fix

This branch portals the mobile menu to the document body so iOS Safari does not constrain the fixed overlay to the backdrop-filtered sticky header. It also replaces the page-specific announcement transformation with a collection-aware, emoji-free presentation.
